## 代码规范

eslint

在项目根目录下添加两个文件 .eslintrc 和 .eslintignore，在文件中进行配置则可，配置方法按照需要去填写。

## git 提交规范

使用 validate-commit-msg 库。

虽然上述两个库可以提供完善的校验功能，但是是需要执行命令才能得到结果，如果使用者不进行校验，这个工具就是形同虚设的，为了将工具及时地使用，就需要使用 ghooks 这个库将校验逻辑编入执行流程。

